export enum ERRORS {
  NO_MATCHING_USER = 'No matching user found',
  INVALID_CREDENTIALS = 'Invalid credentials',
  INVALID_SIGNUP_DATA = 'You must provide an email and a password',
  EMAIL_IN_USE = 'Email in use',
};
